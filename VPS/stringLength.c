#include <stdio.h>
#include <string.h>

int main()
{
    char a[20] = {"VPS Computer"};
    char b[20] = {'C', ' ', 'L', 'a', 'n', 'g', 'u', 'a', 'g', 'e'};
    printf("Length of a is %d.\n", strlen(a));
    printf("Length of b is %d.\n", strlen(b));
    return 0;
}