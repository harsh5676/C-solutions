#include <stdio.h>
#include <conio.h>

int main()
{
    int x = 2;
    if (x == 2 && x != 0) //  if(x == 2 && x != 0);
        printf("Hello\n");
    else
        printf("Bye\n");
    getch();
    return 0;
}